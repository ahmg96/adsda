<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wi_projekt">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WIprojekt">
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2" />
        <activity
            android:name=".Einstellungen"
            android:label="@string/title_activity_einstellungen" />
        <activity android:name=".Registrierung" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Umsatz_Vormerken" />
        <activity android:name=".Jahresbudget" />
        <activity android:name=".Einkauf_aufnehmen" />
        <activity android:name=".Monatsbudget" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>